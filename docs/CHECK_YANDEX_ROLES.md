# ✅ Проверка ролей Yandex Cloud

## Как проверить, что роли назначены правильно

### 1. Через веб-интерфейс

**Проверка на каталоге:**

1. [console.cloud.yandex.ru](https://console.cloud.yandex.ru/)
2. Каталоги → ваш каталог `b1g0039n255v028lq8c1`
3. Вкладка "Права доступа"
4. Найдите `cloud-a-s-aleksejev`
5. Должны быть роли:
   ```
   ✅ ai.speechkit-stt.user
   ✅ ai.languageModels.user
   ```

**Проверка на сервисном аккаунте:**

1. Сервисные аккаунты → `cloud-a-s-aleksejev`
2. Вкладка "Роли" или "Права доступа"
3. Здесь могут быть другие роли (например, `resource-manager.clouds.owner`)

### 2. Через CLI

```bash
# Список всех ролей сервисного аккаунта
yc iam service-account list-access-bindings cloud-a-s-aleksejev

# Или посмотреть роли на каталоге
yc resource-manager folder list-access-bindings b1g0039n255v028lq8c1
```

### 3. Тест через API

```bash
cd /Users/nybble/projects/tokobot

# Запустите тест
./scripts/test-yandex-stt-direct.sh

# Должно показать:
# ✅ YandexGPT работает
# ✅ SpeechKit доступен (или работает)
```

## Правильная структура ролей

### На КАТАЛОГЕ (b1g0039n255v028lq8c1):

- ✅ `ai.speechkit-stt.user` → сервисный аккаунт `cloud-a-s-aleksejev`
- ✅ `ai.languageModels.user` → сервисный аккаунт `cloud-a-s-aleksejev`

### На ОБЛАКЕ (cloud-alekseev):

- ✅ `resource-manager.clouds.owner` → ваш личный аккаунт
- (опционально) другие административные роли

## Что делать после назначения ролей

1. **Подождите 1-2 минуты** (роли применяются с задержкой)

2. **Перезапустите тест:**

   ```bash
   cd /Users/nybble/projects/tokobot
   ./scripts/test-yandex-stt-direct.sh
   ```

3. **Если тест показал ✅** - запустите бота:

   ```bash
   npm run dev
   ```

4. **Отправьте голосовое сообщение** 🎤 в Telegram

## Если всё ещё не работает

1. **Пересоздайте API ключ** ещё раз (возможно, ключ кэшируется)
2. **Проверьте, что роли назначены НА КАТАЛОГЕ**, а не на сервисном аккаунте
3. **Убедитесь, что используется правильный FOLDER_ID** в .env

## Частые ошибки

❌ **Роли назначены на сервисном аккаунте** вместо каталога
→ Переназначьте на каталог

❌ **Роли назначены на облаке** вместо каталога  
→ Добавьте роли на каталог (хотя роли на облаке должны наследоваться)

❌ **API ключ создан до назначения ролей**
→ Пересоздайте API ключ

❌ **Неправильный FOLDER_ID в .env**
→ Проверьте, что используется `b1g0039n255v028lq8c1`
