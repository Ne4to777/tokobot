# 🕐 Автоматическая отправка идей по расписанию

Бот может автоматически отправлять AI-идеи в указанный чат по расписанию.

## 📅 Расписание по умолчанию

**Каждый день в 12:00 по МСК** (9:00 UTC)

## 🚀 Быстрая настройка

### 1. Получи свой Chat ID

Отправь любое сообщение своему боту в Telegram, затем запусти:

```bash
./scripts/get-chat-id.sh
```

Ты увидишь свой `chat_id` (например: `776265475`)

### 2. Добавь Chat ID в GitHub Secrets

1. Зайди в свой репозиторий на GitHub
2. Settings → Secrets and variables → Actions
3. Нажми **New repository secret**
4. Name: `DAILY_IDEAS_CHAT_ID`
5. Value: твой `chat_id` (например: `776265475`)
6. Save

**Примечание:** `BOT_TOKEN` не нужен в GitHub Secrets — он уже настроен в Vercel.

### 3. Готово! ✅

GitHub Actions автоматически будет отправлять `/idea` каждый день в 12:00 МСК.

---

## ⚙️ Настройка расписания

Если хочешь изменить время отправки, редактируй `.github/workflows/daily-idea.yml`:

```yaml
schedule:
  # Примеры:
  - cron: "0 9 * * *" # Каждый день в 12:00 МСК (9:00 UTC)
  - cron: "0 6 * * *" # Каждый день в 9:00 МСК (6:00 UTC)
  - cron: "0 */6 * * *" # Каждые 6 часов
  - cron: "0 9 * * 1" # Каждый понедельник в 12:00 МСК
```

### Синтаксис cron:

```
* * * * *
│ │ │ │ │
│ │ │ │ └─── День недели (0-6, 0 = воскресенье)
│ │ │ └───── Месяц (1-12)
│ │ └─────── День месяца (1-31)
│ └───────── Час (0-23)
└─────────── Минута (0-59)
```

**Примеры:**

- `0 9 * * *` — каждый день в 9:00 UTC
- `0 9 * * 1-5` — по будням в 9:00 UTC
- `0 9,18 * * *` — каждый день в 9:00 и 18:00 UTC
- `*/30 * * * *` — каждые 30 минут (не рекомендуется!)

---

## 📨 Отправка в разные чаты

### Отправка в личку себе

Используй свой личный `chat_id` (например: `776265475`)

### Отправка в канал

1. Добавь бота в канал как администратора
2. Получи `chat_id` канала (обычно начинается с `-100`, например: `-1001234567890`)
3. Используй этот `chat_id` в `DAILY_IDEAS_CHAT_ID`

### Отправка в группу

1. Добавь бота в группу
2. Получи `chat_id` группы (отрицательное число, например: `-987654321`)
3. Используй этот `chat_id` в `DAILY_IDEAS_CHAT_ID`

---

## 🎯 Отправка с конкретной темой

Если хочешь ежедневные идеи только по определенной теме (например, `sales`),
измени body запроса в `.github/workflows/daily-idea.yml`:

```yaml
run: |
  curl -X POST "${VERCEL_URL}/api/daily-idea" \
    -H "Content-Type: application/json" \
    -d "{\"chat_id\": \"${CHAT_ID}\", \"topic\": \"sales\"}"
```

### Доступные темы:

- `"topic": "sales"` — продажи
- `"topic": "marketing"` — маркетинг
- `"topic": "hr"` — HR и рекрутинг
- `"topic": "product"` — разработка продуктов
- `"topic": "support"` — поддержка клиентов
- `"topic": "finance"` — финансы
- Без `topic` — случайная тема

---

## 🧪 Тестирование

### Запуск вручную

1. Зайди в GitHub → Actions
2. Выбери **Daily AI Idea** workflow
3. Нажми **Run workflow**
4. Проверь Telegram — должна прийти идея!

---

## 🔕 Отключение автоматических идей

### Временно (пауза)

1. GitHub → Actions → Daily AI Idea
2. Нажми три точки `⋯` → **Disable workflow**

### Навсегда (удаление)

Удали файл `.github/workflows/daily-idea.yml`

---

## ❓ FAQ

### Q: Идеи не приходят, что делать?

**A:** Проверь:

1. ✅ `DAILY_IDEAS_CHAT_ID` добавлен в GitHub Secrets
2. ✅ `BOT_TOKEN` добавлен в GitHub Secrets
3. ✅ Бот не заблокирован в чате
4. ✅ В GitHub Actions нет ошибок (Actions → Daily AI Idea → последний запуск)

### Q: Можно ли отправлять идеи в несколько чатов?

**A:** Да! Дублируй блок `curl` в workflow:

```yaml
run: |
  # Личка
  curl -X POST "https://api.telegram.org/bot${BOT_TOKEN}/sendMessage" \
    -d "{\"chat_id\": \"776265475\", \"text\": \"/idea\"}"

  # Канал
  curl -X POST "https://api.telegram.org/bot${BOT_TOKEN}/sendMessage" \
    -d "{\"chat_id\": \"-1001234567890\", \"text\": \"/idea\"}"
```

### Q: Можно ли отправлять разные темы в разное время?

**A:** Да! Создай несколько workflow файлов:

- `.github/workflows/daily-idea-sales.yml` (sales в 9:00)
- `.github/workflows/daily-idea-marketing.yml` (marketing в 15:00)

### Q: GitHub Actions бесплатны?

**A:** Да, для публичных репозиториев — неограниченно.  
Для приватных — 2000 минут/месяц бесплатно (этого хватит на годы cron запусков).

---

## 📊 Примеры настроек

### 1. Ежедневная идея в личку в 9:00 МСК

```yaml
schedule:
  - cron: "0 6 * * *" # 6:00 UTC = 9:00 МСК
```

`DAILY_IDEAS_CHAT_ID` = твой личный chat_id

### 2. Идея по продажам каждый будний день в 10:00 МСК

```yaml
schedule:
  - cron: "0 7 * * 1-5" # 7:00 UTC = 10:00 МСК, пн-пт

# В команде:
"text": "/idea sales"
```

### 3. Случайная идея 2 раза в день (утро и вечер)

```yaml
schedule:
  - cron: "0 6,15 * * *" # 9:00 и 18:00 МСК
```

### 4. Идея в канал каждый понедельник в 12:00 МСК

```yaml
schedule:
  - cron: "0 9 * * 1" # Понедельник, 9:00 UTC = 12:00 МСК
```

`DAILY_IDEAS_CHAT_ID` = chat_id твоего канала (например: `-1001234567890`)

---

## 🎉 Готово!

Теперь твой бот будет автоматически присылать свежие AI-бизнес идеи каждый день! 🚀
