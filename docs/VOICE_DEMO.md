# 🎤 Демонстрация голосовой функции

## Краткий обзор

Это **демонстрационная фича**, показывающая возможности работы с голосовыми сообщениями на русском языке в Telegram боте.

## Что она делает?

1. **Принимает голосовое сообщение** от пользователя (до 30 секунд)
2. **Распознаёт русскую речь** через Yandex SpeechKit API
3. **Извлекает тему** из распознанного текста (продажи, маркетинг, HR и т.д.)
4. **Генерирует бизнес-идею** на основе темы с использованием AI

## Демо сценарий использования

### Шаг 1: Запись голосового сообщения

Пользователь открывает бота в Telegram и записывает голосовое сообщение:

```
🎤 "Придумай идею для маркетинга с искусственным интеллектом"
```

### Шаг 2: Обработка

Бот отвечает последовательно:

```
🎤 Слушаю ваше сообщение...

🔍 Распознаю вашу речь...

📝 Распознано: "придумай идею для маркетинга с искусственным интеллектом"

💡 Генерирую бизнес-идею на основе вашего запроса...
```

### Шаг 3: Результат

```
🎯 Бизнес-идея по теме "marketing":

AI-креатор рекламных кампаний: автоматически генерирует креативы,
тексты объявлений, подбирает таргетинги и запускает A/B тесты
во всех рекламных каналах (Google Ads, Facebook, VK). Модель
обучена на 10000+ успешных кампаний и предсказывает CTR с
точностью 85%.

🎤 Отправьте голосовое сообщение для новой идеи!
```

## Примеры голосовых запросов

### Пример 1: Продажи

```
🎤 "Идея для отдела продаж"
```

**Ожидаемый результат:**

- Распознанный текст: "идея для отдела продаж"
- Тема: sales
- Идея: AI-SDR для автоматизации продаж

### Пример 2: HR

```
🎤 "Что-нибудь для HR специалистов"
```

**Ожидаемый результат:**

- Распознанный текст: "что-нибудь для hr специалистов"
- Тема: hr
- Идея: AI-рекрутер для автоматического подбора кандидатов

### Пример 3: Финансы

```
🎤 "Финансовый продукт с AI"
```

**Ожидаемый результат:**

- Распознанный текст: "финансовый продукт с ai"
- Тема: finance
- Идея: AI-бухгалтер для автоматизации учета

### Пример 4: Свободная формулировка

```
🎤 "Придумай что-нибудь интересное для стартапа"
```

**Ожидаемый результат:**

- Распознанный текст: "придумай что-нибудь интересное для стартапа"
- Тема: извлекается из текста или используется "sales" по умолчанию
- Идея: Случайная AI-first бизнес-идея

## Технические детали

### Архитектура

```
┌──────────────┐
│  Пользователь│
│  Голосовое   │
│  сообщение   │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│  Telegram    │
│  Bot API     │
└──────┬───────┘
       │
       ▼
┌──────────────────────────────────────────┐
│  voice.handler.ts                        │
│  1. Скачивает .ogg файл                  │
│  2. Конвертирует в Buffer                │
└──────┬───────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│  yandex-stt.service.ts                   │
│  POST /speech/v1/stt:recognize           │
│  - Отправляет аудио                      │
│  - Получает текст                        │
│  - Возвращает confidence                 │
└──────┬───────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│  voice.handler.ts                        │
│  extractTopicFromText()                  │
│  - Ищет ключевые слова                   │
│  - Определяет тему                       │
└──────┬───────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│  ai.service.ts / generateIdea()          │
│  - Генерирует идею по теме               │
│  - Использует YandexGPT/Groq/Local       │
└──────┬───────────────────────────────────┘
       │
       ▼
┌──────────────┐
│  Ответ       │
│  пользователю│
└──────────────┘
```

### Использованные API

1. **Telegram Bot API**

   - `getFileLink()` - получение ссылки на файл
   - `fetch()` - скачивание OGG файла

2. **Yandex SpeechKit API**

   - Endpoint: `https://stt.api.cloud.yandex.net/speech/v1/stt:recognize`
   - Формат: OGG Opus (нативный формат Telegram)
   - Язык: ru-RU

3. **AI Generation API**
   - YandexGPT / Groq / Gemini
   - Генерация бизнес-идей

## Ограничения

### Технические

- ⏱ **Максимальная длительность:** 30 секунд (ограничение синхронного API)
- 🗣 **Язык:** Только русский (можно расширить на другие языки)
- 📊 **Формат:** OGG Opus (автоматически от Telegram)

### Квоты

- 🆓 **Бесплатно:** 1000 запросов/месяц (Yandex SpeechKit)
- 💰 **После лимита:** $0.0015 за 15 секунд аудио

### Качество распознавания

- ✅ Отличное: Чёткая речь, тихий фон
- ⚠️ Среднее: Акцент, шум, быстрая речь
- ❌ Плохое: Очень шумно, неразборчиво

## Зачем это нужно?

### Use Cases

1. **Hands-free взаимодействие**

   - Пользователь за рулём
   - Пользователь занят делами
   - Удобнее говорить, чем печатать

2. **Демонстрация AI возможностей**

   - Показывает интеграцию с SpeechKit
   - Демонстрирует обработку естественного языка
   - Пример end-to-end AI workflow

3. **Повышение вовлечённости**
   - Необычный способ взаимодействия
   - Снижает барьер для запроса идей
   - Делает бота более "живым"

### Возможные расширения

1. **Text-to-Speech ответы**

   ```typescript
   // Бот отвечает голосом
   const audioBuffer = await synthesizeSpeech(ideaText);
   await ctx.replyWithVoice({ source: audioBuffer });
   ```

2. **Диалоговый режим**

   ```typescript
   // Серия уточняющих вопросов голосом
   await ctx.reply("Для какой индустрии?");
   // ... ждём голосовой ответ
   await ctx.reply("Какой бюджет?");
   // ... генерация с учётом контекста
   ```

3. **Анализ эмоций**

   ```typescript
   // Определение тона голоса
   const emotion = await analyzeVoiceEmotion(audioBuffer);
   // Адаптация ответа под настроение
   ```

4. **Многоязычность**
   ```typescript
   // Определение языка автоматически
   const language = await detectLanguage(audioBuffer);
   await recognizeSpeech({ languageCode: language });
   ```

## Как протестировать?

### Локально

```bash
# 1. Установите зависимости
npm install

# 2. Настройте .env
cp env.example .env
# Добавьте:
# BOT_TOKEN=...
# YANDEX_API_KEY=...
# YANDEX_FOLDER_ID=...

# 3. Запустите бота
npm run dev

# 4. Отправьте голосовое сообщение в Telegram
```

### На продакшене (Vercel)

```bash
# 1. Деплой на Vercel
vercel --prod

# 2. Установите webhook
curl "https://api.telegram.org/bot<TOKEN>/setWebhook?url=<VERCEL_URL>/api/webhook"

# 3. Отправьте голосовое сообщение
```

### Тестовый скрипт

```bash
# Запустите готовый скрипт проверки
chmod +x scripts/test-voice.sh
./scripts/test-voice.sh
```

## FAQ

**Q: Почему только 30 секунд?**  
A: Используется синхронное API для минимальной задержки. Для длинных аудио можно использовать Streaming API.

**Q: Работает ли без интернета?**  
A: Нет, требуется подключение к Yandex SpeechKit API.

**Q: Можно ли использовать другие языки?**  
A: Да! Измените `languageCode` на `en-US`, `de-DE` и т.д.

**Q: Сколько это стоит?**  
A: Первые 1000 запросов/месяц бесплатно, далее $0.0015 за 15 секунд.

**Q: Работает ли из России?**  
A: ✅ Да! Yandex Cloud доступен в РФ без VPN.

**Q: Можно ли добавить голосовые ответы?**  
A: Да! Используйте Yandex TTS API. См. раздел "Расширение" в [VOICE_RECOGNITION.md](VOICE_RECOGNITION.md).

## Полная документация

Для подробной информации см. [VOICE_RECOGNITION.md](VOICE_RECOGNITION.md)

---

**Попробуйте прямо сейчас!** 🎤

Отправьте голосовое сообщение боту и посмотрите, как работает AI-распознавание речи!
