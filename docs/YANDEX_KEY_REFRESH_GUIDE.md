# 🔑 Пересоздание API ключа Yandex Cloud

## Проблема

Роль `ai.speechkit-stt.user` есть, но API ключ всё равно даёт 401 Permission Denied.

## Причина

API ключ был создан **ДО** добавления роли SpeechKit. Старые ключи не подхватывают новые роли автоматически.

## Решение: Пересоздайте API ключ

### Шаг 1: Удалите старый ключ (опционально)

1. Откройте [console.cloud.yandex.ru](https://console.cloud.yandex.ru/)
2. Сервисные аккаунты → `cloud-a-s-aleksejev`
3. Вкладка "API-ключи"
4. Найдите ключ `AQVNzFOQ...` и удалите его

### Шаг 2: Создайте новый API ключ

1. На той же странице нажмите **"Создать API-ключ"**
2. Скопируйте новый ключ (он покажется только один раз!)
3. Сохраните его в безопасном месте

### Шаг 3: Обновите .env файл

```bash
# Замените старый ключ на новый
YANDEX_API_KEY=YOUR_NEW_API_KEY_HERE
```

### Шаг 4: Перезапустите бота

```bash
# Остановите бота (Ctrl+C)
# Запустите снова
npm run dev
```

### Шаг 5: Проверьте работу

```bash
# Тест API
./scripts/test-yandex-stt-direct.sh

# Если показывает ✅ - всё работает!
```

## Альтернатива: Подождите 5-10 минут

Иногда права применяются с задержкой. Попробуйте:

1. Подождать 5-10 минут
2. Перезапустить бота
3. Попробовать снова

## Проверка текущих ролей

Убедитесь, что обе роли назначены:

```
✅ ai.speechkit-stt.user          (для распознавания речи)
✅ ai.languageModels.user         (для YandexGPT)
```

Если второй роли нет, добавьте её тоже!
