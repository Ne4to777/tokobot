{
  "name": "tokobot",
  "version": "1.0.0",
  "description": "Telegram bot for generating ideas using free AI models",
  "type": "module",
  "scripts": {
    "dev": "tsx watch api/webhook.ts"
  },
  "dependencies": {
    "telegraf": "^4.15.6",
    "dotenv": "^16.3.1",
    "@types/node": "^20.10.6"
  },
  "engines": {
    "node": ">=18.x"
  }
}

